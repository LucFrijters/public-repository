# System Configuration Examples

This directory contains example system configuration files.

## Available Configurations

- `gitconfig` - Git global configuration example
- `ssh-config` - SSH client configuration example (non-sensitive)
- `bash-aliases` - Useful bash aliases

## Usage

Copy and customize these configurations for your system. Remember to never include sensitive information like passwords or private keys.
